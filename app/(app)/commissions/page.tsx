import { ComingSoon } from "@/components/coming-soon";

export default function CommissionsPage() {
  return (
    <ComingSoon
      phase={5}
      title="Commissions"
      description="Signups, residuals, sale events, overrides, and pay periods."
    />
  );
}
